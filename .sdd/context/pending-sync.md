# Pending GitHub Sync

`GH_AVAILABLE=0` — no git remote configured when `epic-sync` ran for `exam-builder-base`. Epic-sync step was skipped entirely (no `gh` calls attempted). Run this once a GitHub remote + `gh auth login` are set up:

- Create epic issue from `.sdd/epics/exam-builder-base/epic.md` (frontmatter stripped)
- Create task issues from `.sdd/epics/exam-builder-base/{001,002,003,010,011,012,020,090}.md`
- Record mapping in `.sdd/epics/exam-builder-base/github-mapping.md`

Logged: 2026-09-21T09:22:14Z
