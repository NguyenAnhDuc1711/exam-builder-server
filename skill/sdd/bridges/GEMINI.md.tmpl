<!-- SDD:START -->
# SDD — Project Manager

Spec-driven development workflows. When the user invokes a `/sdd:` or `/pm:` command, read the skill entry point and route to the matching reference doc.

## Skill Entry Point

@skill/sdd/SKILL.md

## Conventions

@skill/sdd/references/conventions.md

## Runtime Data

All workflow state is stored in `.sdd/` (prds, epics, context, verify, config, sessions, qa, progress). Never write to `.claude/`.
<!-- SDD:END -->
